import { PlaceholderPage } from "@/components/workspace/placeholder-page";

export default function MemoryPage() {
  return (
    <PlaceholderPage
      icon="memory"
      barTitle="Memory"
      title="Memory — Phase 4"
      description="Document ingestion, embeddings, and vector search with citations."
    />
  );
}
