# Fine-tune Helix-style chat models with Unsloth (LoRA / QLoRA).
# Install Unsloth separately first — see finetune/README.md (Colab vs local differ).

torch>=2.4.0
transformers>=4.46.0
datasets>=3.0.0
trl>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
peft>=0.13.0
huggingface_hub>=0.26.0
